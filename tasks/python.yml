- name: Install Python 3 
  apt:
    name: "{{ item }}"
    state: present
  loop:
    - python3
    - python3-venv 
    - python3-pip
    - python3-requests 
    - python3-numpy
  tags: [ 'packages', 'python' ]
